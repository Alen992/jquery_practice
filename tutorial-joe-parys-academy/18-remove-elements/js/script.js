$(function() {
  // const removeListItem = $('li').remove();
  // $('form').children().not('textarea, input[type=text], br').remove();

  // const detachedListItem = $('li').detach();
  // console.log(detachedListItem);
  // $('#content').append(detachedListItem);

$('p:first').empty();

$('.red-box, .green-box, .blue-box ').empty();
});
